function newAttribute(){
	// display a key value pair input view
};

function saveAttribute(){
	// add the key value pair to the doc object and view
}

function saveDocuments(){
	// create a new model with the doc 
}